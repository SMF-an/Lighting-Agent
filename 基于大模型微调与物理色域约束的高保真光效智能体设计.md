# 基于大模型微调与物理色域约束的高保真光效智能体设计

## 赛题背景与核心任务

随着智能家居与商用照明的发展，如何将抽象的语言需求转化为具体的物理灯光效果成为一大挑战。本任务的核心是一套从“文本 Prompt 到光效图像生成”，再将“图像用于实际物理灯具渲染”的完整流转体系。但在实际工程落地中存在巨大壁垒：物理真实灯具的发光 RGB 色域是极其有限的，且硬件发光面的形状、布局与物理尺寸各异，这导致 AI 随意生成的绚丽概念图，往往因为存在“遵循指令效果差”、“超色域”或“反物理光晕”而无法被真实灯具完美复现。

为了解决这一问题并构建完善的评价体系，本项目将通过收集大量的家庭场景图像、语音指令和文本描述，涵盖不同房间类型和不同使用场景需求。这些数据将作为模型测试的输入，用于评估模型对各种模态数据的理解和处理能力。并通过构建多样化的灯光照明场景数据集，包括不同灯光亮度、颜色、色温、布局等参数的组合，用于测试模型生成的灯光照明方案的合理性和可行性。最后收集用户对灯光效果的反馈数据，包括用户对不同场景下灯光亮度、颜色、氛围等方面的评价和意见，用于评估模型的智能调试功能和场景推荐方案的用户满意度。

基于上述背景，本赛题要求参赛队伍打造一条“自动化且物理可落地”的灯光设计流水线。你们需要串联“大语言模型”、“文生图微调”、“图像色彩处理”三大技术，将一段普通的场景文字描述，转化为一幅既符合场景情绪、又严格受限于特定硬件发光物理极限的高保真光效效果图。

**输入文本：** 酒店套房客厅，浪漫氛围。
**输出图片：** ![example1](imgs/example1.png)

**输入文本：** 酒店酒吧，抽象霓虹样式光，用于微醺、社交、停留延长。
**输出图片：** ![example2](imgs/example2.png)

为了让参赛者更直观地理解上述任务，上面提供了两个典型场景的“文本到光效”转译示例。这两个示例分别代表了本赛题的两个核心考察维度：
- **示例一（情绪解构与平滑生成）：**
针对“酒店套房客厅，浪漫氛围”这类高度抽象的感官诉求，系统需具备将其精准解构的能力。如第1个例子所示，模型需要将其转译为视觉上的柔和渐变。在此场景下，模型需生成无噪点、自然过渡光晕的渐变图片，确保视觉上带来极度舒适的连贯性。
- **示例二（商业光效生成）：**
针对“酒店酒吧，抽象霓虹样式光...”这类强视觉冲击的需求，模型生成了如第2个例子所示的高饱和度紫粉色渐变。

## 核心开发任务与步骤拆解

参赛队伍可参考以下6个模块进行代码编写与系统搭建：

### 模块一：场景解构与光效 Prompt 转译

编写一个基于 LLM 的智能体脚本，将普通场景描述自动转化为可用于生图的光效提示词（见附件）。

**示例：**
- **输入：** "当前场景是一个飘香的咖啡厅，人们在这里能够惬意地品尝咖啡。硬件规格尺寸为1220mm*370mm。"
- **输出：** "Soft gradient lighting, warm sunset hues transitioning from amber to soft magenta, cozy and relaxing atmosphere, cinematic lighting, 8k resolution..."

智能体需具备场景情绪分析能力。

### 模块二：光效数据集构建与文生图模型微调

让图像大模型"学会"生成平滑渐变光效，而不是生硬的打光。参赛队伍可使用赛题方提供的具有"渐变光效"特征的场景图片进行模型训练，交付物为训练好的模型权重文件。

**示例：**
- **训练前：** 模型接收到 "gradient lighting" 关键词时生成的图像光斑生硬、有明显噪点
- **训练后：** 同样关键词下，模型生成光晕自然、色彩过渡平滑的渐变光效

### 模块三：自动化推理生图 

将前两个模块串联，实现一键生图。加载基础大模型及微调权重，接收模块一生成的 Prompt，自动输出概念光效图。

**注意：** 此阶段生成的图片往往色彩过于鲜艳，包含现实 LED 混光无法实现的色彩（超色域），需后续模块处理。

### 模块四：物理色域约束与 SDL 映射算法

编写算法，将 AI 生成的"假光"压缩成物理可实现的"真光"。

赛题方将提供一个模拟的"欧普智能灯具色域边界坐标文件"（例如定义了在 CIE 1931 色度图上该硬件所能显示的最大色彩多边形边界），参赛队伍需完成色彩空间转换、色域检测和色域映射三步处理。

**核心难点：** 直接裁剪超域像素会导致渐变光效出现严重的"断层（Banding）"或色块。参赛者需要设计平滑的映射算法，在把颜色拉回物理边界内的同时，保留原有的光效渐变平滑度。

**示例：**
- **映射前：** 图中某区域出现饱和度极高的品红渐变，超出灯具物理色域
- **映射后：** 该区域颜色被平滑拉回至色域边界内，渐变过渡依然自然连续，无可见断层

### 模块五：系统测试、结果评价与分析

任务目标： 测试模型的泛化生成能力，并建立客观与主观结合的评价链路。

- 核心评测流程：
  1. **给定输入条件：** 赛题方在测试阶段，将给定一组全新的测试数据（包含各类新的场景 Prompt）。
  2. **自动化生成：** 参赛队伍的 Pipeline 需根据新的 Prompt ，自动执行模块一至四，输出对应的高保真渲染图。
  3. **联合评价机制：** 最终的生成结果将由“大模型评估 + 专家评估”双管齐下：
    - 大模型客观评价：利用多模态大模型结合客观数据，对图像的光效合理性、是否严格遵循色域约束进行打分。
    - 专家主观评价：结合前期收集的用户真实反馈数据，由专家团队从光效氛围营造、工程落地可行性、场景推荐满意度等维度进行盲评打分。
- 功能要求 (Demo展示)：
  1. 用户在界面输入全新的场景词和指定灯具硬件信息。
  2. 界面展示中间结果（光效 Prompt、未约束的 Raw 图像）。
  3. 展示最终结果对比 (A/B Test)： 左边放未经色彩约束的 Raw 图，右边放经过模块四处理的高保真图以供评阅。

## 补充材料

### 注：因训练数据较少，需要人为构造训练数据，可参考以下 pipeline

图像数据集是基于色彩学原理，可完全通过代码程序化生成的，可分为两个层级：
- 基础数据（双色线性光影）：通过算法生成对比和谐的双色彩对，并引入微弱的高斯中心雾化和边缘暗角，构建平滑且具有基础空间感的背景。
参考代码：[simulate_graph_one.py](src/simulate_graph_one.py)
- 高级数据（多色流体光晕）：我们在算法中融入了专业的色彩学模型（如类比色、分裂互补色），并加上统一的“空气感修饰”。利用多点流体和网格权重渲染，生成极具现代设计感、结构复杂的弥散光背景。
参考代码：[simulate_graph_more.py](src/simulate_graph_more.py)
- 构造优势：这种纯数学矩阵生成的方法，既保证了图片绝对高清、零噪点、色彩不脏，又能通过修改随机种子无限量规模化生成。
- 生成图片之后，用视觉模型生成描述，完成图文对数据构建。

### 注：有关于模块一的补充材料

#### 预期效果

- **输入文本：** 零售店试妆区，柔和渐变光效，用于营造放松、沉浸的氛围
- **输出文本：** 柔和的渐变灯光从淡黄色过渡到浅粉色，为零售化妆品测试区营造出放松而沉浸的氛围。

- **输入文本：** 餐厅包间，暖色流动光，用于营造亲密感、仪式感
- **输出文本：** 温暖流动的光线呈现出柔和的黄色和浅橙色渐变，营造出一种亲密而庄重的氛围，增强了用餐空间的舒适感。

- **输入文本：** 办公空间，清新自然光，用于营造活力、专注的感觉
- **输出文本：** 明亮的温暖色调，浅黄色和淡橙色的光线营造出一种清新而振奋的氛围，提升了办公室空间的专注力和活力。